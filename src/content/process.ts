// Stage data is in messages/{ko,en}.json under process.stages.
// This file just exposes the count so components can render numbered nodes
// without re-reading translation arrays.
export const PROCESS_STAGE_COUNT = 4;
