// Reserved for future use — the new Mercury Hero replaces the pipeline visual
// with an atmospheric backdrop. Keeping this stub in case we want to add a
// product UI mockup further down the page.
export function FlowingPipeline() {
  return null;
}
